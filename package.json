{
  "name": "passgen",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd backend && npm run build",
    "start": "cd backend && npm start"
  }
}
